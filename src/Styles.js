const app = {
    textAlign: "center"
}

const appHeader = {
    height: '50px',
    padding: "20px",
    color: "white",
    backgroundColor: "#222"
  }

const appTitle = {
    fontSize: "1.5em"
  }

const playersList = {
    display: 'flex',
    alignItems: 'center'
}

const player = {
    fontSize: "18px",
    border: "2px solid #999",
    borderRadius: "5px",
    width: "100px",
    padding: "15px",
    margin: "15px",
    boxShadow: "2px 2px #aaa"
}

const button = {
    height: '42px'
}

export default {
    app,
    button,
    appTitle,
    appHeader,
    playersList,
    player
};