const app = {
    textAlign: "center"
}

const appHeader = {
    height: '50px',
    backgroundColor: "#222",
    padding: "20px",
    color: "white"       
  }

const appTitle = {
    fontSize: "1.5em"
  }

export default {
    app,
    appTitle,
    appHeader
};